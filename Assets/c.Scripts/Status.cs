
// move status
public enum MovePosition {
	Stay =0,
	Left,
	Right
}

// bounce status
public enum Bouncy {
	Ready=0,
	Down,
	Up,
	Not,
	Wait,
	Twin
}

// game status
public enum GameSet {
	play =0,
	win,
	lose
}

// fish
public enum fishMove {
	wait=0,
	up,
	down,
	bite
}

//crocodile
public enum crocodileMove {
	normal=0,
	angly
}